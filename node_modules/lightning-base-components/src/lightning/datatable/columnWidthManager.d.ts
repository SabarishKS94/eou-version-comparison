import { AutoWidthStrategy } from './autoWidthStrategy';
import { FixedWidthStrategy } from './fixedWidthStrategy';
import { ColumnDefinition, DatatableState, RowData } from './types';
import { TotalWidthsData } from './widthManagerShared';
import { DatatableInterface } from './autoWidthStrategy';
import LightningDatatable from './datatable';
declare const AUTO_WIDTH_MODE = "auto";
declare const FIXED_WIDTH_MODE = "fixed";
export interface AdjustedWidths {
    columnWidths: number[];
    expectedTableWidth?: number;
}
type WidthMode = typeof AUTO_WIDTH_MODE | typeof FIXED_WIDTH_MODE;
interface WidthStrategies {
    [FIXED_WIDTH_MODE]: FixedWidthStrategy;
    [AUTO_WIDTH_MODE]: AutoWidthStrategy;
}
export declare function updateColumnWidths(columns: ColumnDefinition[], widthsData: TotalWidthsData, adjustedWidths: AdjustedWidths): void;
export declare class ColumnWidthManager {
    columnWidthMode: WidthMode;
    widthStrategies: WidthStrategies;
    isResizingUpdateQueued: boolean;
    isAutoResizingUpdateQueued: boolean;
    constructor(widthsData: TotalWidthsData);
    get columnWidthStrategy(): FixedWidthStrategy | AutoWidthStrategy;
    set minColumnWidth(value: number);
    set maxColumnWidth(value: number);
    set wrapTextMaxLines(value: number);
    shouldFireResizeEvent(previousWidthsData: TotalWidthsData, columns: ColumnDefinition[]): boolean;
    adjustColumnsSize(template: ShadowRoot, refs: Record<string, Element>, columns: ColumnDefinition[], widthsData: TotalWidthsData): void;
    adjustColumnsSizeAfterResize(dt: LightningDatatable, columns: ColumnDefinition[], widthsData: TotalWidthsData): void;
    handleDataChange(columns: ColumnDefinition[]): void;
    handleColumnsChange(columns: ColumnDefinition[]): void;
    handleWidthModeChange(columns: ColumnDefinition[]): void;
    handleRowNumberOffsetChange(state: DatatableState, widthsData: TotalWidthsData): void;
    handleCheckboxColumnChange(previousValue: boolean, newValue: boolean, columns: ColumnDefinition[]): void;
    handleActionsColumnChange(previousValue: boolean, newValue: boolean, columns: ColumnDefinition[]): void;
    handleRowNumberColumnChange(previousValue: number, newValue: number, columns: ColumnDefinition[]): void;
    setAutoResizingUpdate(columns: ColumnDefinition[]): void;
    hasDataChanged(previousData: RowData[], newData: RowData[]): boolean;
    shouldResizeWithUpdate(template: ShadowRoot, refs: Record<string, Element>, widthsData: TotalWidthsData): boolean;
    getDatatableInterface(template: ShadowRoot, refs: Record<string, Element>): DatatableInterface;
}
export {};
//# sourceMappingURL=columnWidthManager.d.ts.map