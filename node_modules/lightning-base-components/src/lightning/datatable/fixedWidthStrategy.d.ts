import { TotalWidthsData } from './widthManagerShared';
import { ColumnDefinition } from './types';
import { DatatableInterface } from './autoWidthStrategy';
import { ColumnWidthMetaData } from './widthManagerShared';
export declare class FixedWidthStrategy {
    widthsData: ColumnWidthMetaData;
    constructor(minColumnWidth: number, maxColumnWidth: number);
    set minColumnWidth(value: number);
    set maxColumnWidth(value: number);
    getAdjustedColumnWidths(datatableInterface: DatatableInterface, _columns: ColumnDefinition[]): {
        columnWidths: number[];
    };
    getFlexibleColumnWidth(widthsData: TotalWidthsData, totalTableWidth: number): number;
}
//# sourceMappingURL=fixedWidthStrategy.d.ts.map