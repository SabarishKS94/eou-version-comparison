import { ColumnDefinition } from './types';
import { TotalWidthsData } from './widthManagerShared';
import { AdjustedWidths } from './columnWidthManager';
interface ColumnWidthsDistribution {
    colsWithMinWidth?: number[];
    colsWithMaxWidth?: number[];
}
export interface DatatableInterface {
    getAvailableWidthFromDom(): number;
    getDataCellWidths(): number[];
    getTableElementWidth(): number;
    getHeaderCellWidths(): number[];
}
export declare class AutoWidthStrategy {
    columnWidthRatios: number[];
    columnWidthData: TotalWidthsData;
    columnWidthsDistribution: ColumnWidthsDistribution;
    constructor(minColumnWidth: number, maxColumnWidth: number, wrapTextMaxLines?: number);
    set minColumnWidth(value: number);
    set maxColumnWidth(value: number);
    set wrapTextMaxLines(value: number);
    getAdjustedColumnWidths(datatableInterface: DatatableInterface, columns: ColumnDefinition[], recomputeAutoWidthRatios?: boolean): AdjustedWidths;
    getRatios(cellWidths: number[], totalWidth: number): number[];
    calculateColumnWidthRatios(datatableInterface: DatatableInterface, columns: ColumnDefinition[], widthsData: TotalWidthsData): void;
    setColumnWidthRatios(datatableInterface: DatatableInterface, dataCellWidths: number[], widthsData: TotalWidthsData): void;
    resetColumnWidthsDistribution(): void;
    distributeWidthFromRatios(availableWidth: number, columns: ColumnDefinition[], widthsData: TotalWidthsData): number[];
    getColumnWidthFromRatio(availableWidth: number, widthsData: TotalWidthsData, colIndex: number): number;
    redistributeExtraWidth(expectedTableWidth: number, columnWidths: number[], columns: ColumnDefinition[]): void;
    redistributeDeficitWidth(expectedTableWidth: number, columnWidths: number[], columns: ColumnDefinition[]): void;
}
export {};
//# sourceMappingURL=autoWidthStrategy.d.ts.map