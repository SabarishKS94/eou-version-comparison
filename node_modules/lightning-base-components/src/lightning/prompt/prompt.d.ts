import LightningInteractiveDialogBase from 'lightning/interactiveDialogBase';
import { parent, instanceName } from 'lightning/overlayUtils';
import LightningPrimitiveOverlay from 'lightning/primitiveOverlay';
type Theme = 'default' | 'shade' | 'inverse' | 'alt-inverse' | 'success' | 'info' | 'warning' | 'error' | 'offline';
export default class LightningPrompt extends LightningPrimitiveOverlay {
    static [instanceName]: string;
    static [parent]: typeof LightningInteractiveDialogBase;
    label: string;
    message: string;
    defaultValue: string;
    variant: 'header' | 'headerless';
    theme: Theme;
    close(result?: unknown): void;
    constructor();
    renderedCallback(): void;
    getOkValue(): string;
}
export {};
//# sourceMappingURL=prompt.d.ts.map