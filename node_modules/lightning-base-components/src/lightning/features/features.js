/*
 * Copyright (c) 2025, Salesforce, Inc.,
 * All rights reserved.
 * For full license text, see the LICENSE.txt file
 */
import gates from './gates/index';
const _features = Object.assign(Object.create(null), gates);
const errorMessage = 'This object and its prototype are immutable. Object values can only be updated with `setFeatureForTest()`';
const features = new Proxy(_features, {
    set() {
        throw new TypeError(errorMessage);
    },
    defineProperty() {
        throw new TypeError(errorMessage);
    },
    deleteProperty() {
        throw new TypeError(errorMessage);
    },
    setPrototypeOf() {
        throw new TypeError(errorMessage);
    },
});
function setFeatureForTest(name, value) {
    if (typeof _features[name] === 'undefined') {
        throw new TypeError(`Unrecognized feature "${name}". Expected one of: ${Object.keys(_features).join(', ')}`);
    }
    _features[name] = value;
}
export { features, setFeatureForTest };
