export namespace address {
    function getAddressInfoForCountry(langCode: string, countryCode: string): {
        fmt: string;
        input: string;
        require: string;
    };
    function getAddressFormat(langCode: string, countryCode: string): string;
    function getAddressInputOrder(langCode: string, countryCode: string): string;
    function getAddressInputOrderAllField(langCode: string, countryCode: string): string;
    function getAddressRequireFields(langCode: string, countryCode: string): string;
    function formatAddressAllFields(langCode: string, countryCode: string, values: {
        address: string;
        country: string;
        city: string;
        state: string;
        zipCode: string;
    }, lineBreak: string): string;
    function formatAddress(langCode: string, countryCode: string, values: {
        address: string;
        country: string;
        city: string;
        state: string;
        zipCode: string;
    }, lineBreak: string): string;
    function buildAddressLines(pattern: string, values: {
        address: string;
        country: string;
        city: string;
        state: string;
        zipCode: string;
    }, lineBreak: string, ignoreEmptyLines: string): string;
    function followReferences(data: any, countryCode: string): any;
    function containsHanScript(...args: string[]): boolean;
    function getCountryFromLocale(langCode: string, countryCode: string, values: any): string;
    function getFallback(langCode: string, countryCode: string, address: any): string;
}
//# sourceMappingURL=AddressFormat.d.ts.map